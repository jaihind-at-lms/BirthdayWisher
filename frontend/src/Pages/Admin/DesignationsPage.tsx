import type { JSX } from 'react'

import MasterPage from '@project/Components/Admin/MasterPage'

const DesignationsPage = (): JSX.Element => (
  <MasterPage tab="designations" title="Designations" />
)
export default DesignationsPage
